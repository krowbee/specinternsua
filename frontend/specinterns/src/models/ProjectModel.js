
export default class Project{
    constructor(title, status, startDate, duration, members, preview){
        this.title = title;
        this.status = status;
        this.startDate = startDate;
        this.duration = duration;
        this.members = members;
        this.preview = preview;
    }
}